{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    <h1>Client</h1>

    {# Ma fontion dans "findClientWithId" du repository me renvoie un tableau
     donc pour y accéder je dois utiliser {{ client[0].id }} 
     ici [0] correspondras tjr à l'utilisateur selectionner car 
     ma requete ne renvoie qu' une ligne #}

    <table class="table">  
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ client[0].id }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ client.roles ? client.roles|json_encode : '' }}</td>
            </tr> #}
            <tr>
                <th>Email</th>
                <td>{{ client[0].email }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ client[0].username }}</td>
            </tr>
            <tr>
                <th>Password</th>
                {# <td>{{ client[0].user_password }}</td> #}
                <td>*******</td> {# voir comment mettre l'affichage auto des ** #}
            </tr>
            <tr>
                <th>NomClient</th>
                <td>{{ client[0].nom_client }}</td>
            </tr>
            <tr>
                <th>PrenomClient</th>
                <td>{{ client[0].prenom_client }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ client[0].telephone }}</td>
            </tr>
        </tbody>
    </table>

    <table>
        {% for adress in adresses %}
            <tr>
                <td>{{ adress.id }}</td>
                <td>{{ adress.numAdresse }}</td>
                <td>{{ adress.rueAdresse }}</td>
                <td>{{ adress.complementAdresse }}</td>
                <td>{{ adress.villeAdresse }}</td>
                <td>{{ adress.codePostpAdressse }}</td>
                <td>{{ adress.paysAdresse }}</td>
                {# <td>{{ adress.id_client_id.id}}</td> #}
                <td>
                    <a href="{{ path('app_adresses_show', {'id': adress.id}) }}">show</a>
                    <a href="{{ path('app_adresses_edit', {'id': adress.id}) }}">edit</a>
                </td>
            </tr>
        {# {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr> #}
        {% endfor %}

    </table>
    
    <a href="{{ path('app_mes_adresses_new', {'idClient': client[0].id}) }}">créer </a>


    {# <a href="{{ path('app_mes_adresses_index', {'idClient': client[0].id}) }}">Mes adresses </a> #}

    <a href="{{ path('app_profil_utilisateurs_index') }}">Retour</a>

    <a href="{{ path('app_profil_utilisateurs_edit', {'idClient': client[0].id}) }}">Modifier</a>

    {{ include('profil_utilisateurs/_delete_form.html.twig') }}
{% endblock %}
